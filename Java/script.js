const header = document.querySelector('.header'), progress = document.querySelector('.progress'), menu = document.querySelector('.menu'), nav = document.querySelector('nav'); function scroll() { header.classList.toggle('scrolled', scrollY > 30); let m = document.documentElement.scrollHeight - innerHeight; progress.style.width = m ? scrollY / m * 100 + '%' : '0%' } addEventListener('scroll', scroll, { passive: true }); scroll(); menu.addEventListener('click', () => { nav.classList.toggle('open'); menu.classList.toggle('active') }); nav.querySelectorAll('a').forEach(a => a.addEventListener('click', () => nav.classList.remove('open'))); const obs = new IntersectionObserver(es => es.forEach(e => { if (e.isIntersecting) { e.target.classList.add('visible'); obs.unobserve(e.target) } }), { threshold: .12 }); document.querySelectorAll('.reveal').forEach((e, i) => { e.style.transitionDelay = (i % 4) * 70 + 'ms'; obs.observe(e) });
